parameters=
fee=0
clientrequestlimit=1000
disabled=0
help=Returns the number of hashes per second that the node is mining with (Note: Will only return 0x0). Usage: xrService ETHhashrate


#private:: Equivalent to: docker exec -t ubuntu /bin/bash ETHhashrate.sh
private::type=docker
private::containername=ubuntu
private::quoteargs=0
private::command=/bin/bash ETHhashrate.sh
private::args=