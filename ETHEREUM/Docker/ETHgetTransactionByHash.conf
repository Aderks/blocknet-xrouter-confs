parameters=string
fee=0
clientrequestlimit=1000
disabled=0
help=Returns the information about a transaction requested by transaction hash. Usage: xrService ETHgetTransactionByHash [tx_hash]


#private:: Equivalent to: docker exec -t ubuntu /bin/bash ETHgetTransactionByHash.sh [tx_hash]
private::type=docker
private::containername=ubuntu
private::quoteargs=0
private::command=/bin/bash ETHgetTransactionByHash.sh
private::args=$1