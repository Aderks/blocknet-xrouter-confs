parameters=
fee=0
clientrequestlimit=1000
disabled=0
help=Returns a list of addresses owned by client (Note: This call will not return any accounts). Usage: xrService ETHaccounts


#private:: Equivalent to: docker exec -t ubuntu /bin/bash ETHaccounts.sh
private::type=docker
private::containername=ubuntu
private::quoteargs=0
private::command=/bin/bash ETHaccounts.sh
private::args=