parameters=
fee=0
clientrequestlimit=1000
disabled=0
help=Returns the EIP155 chain ID used for transaction signing at the current best block. Null is returned if not available. Usage: xrService ETHchainId


#private:: Equivalent to: docker exec -t ubuntu /bin/bash ETHchainId.sh
private::type=docker
private::containername=ubuntu
private::quoteargs=0
private::command=/bin/bash ETHchainId.sh
private::args=